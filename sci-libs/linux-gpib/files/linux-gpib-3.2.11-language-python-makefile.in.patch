--- language/python/Makefile.in.original	2008-01-30 15:09:50.000000000 -0500
+++ language/python/Makefile.in	2008-09-16 14:32:13.000000000 -0400
@@ -370,7 +370,7 @@
 	-{ $(PYTHON) setup.py build && touch build; } || { $(RM) -r build; exit 1; }
 
 install-data-local:
-	-$(PYTHON) setup.py install
+	-$(PYTHON) setup.py install --prefix=$(DESTDIR)$(prefix)
 
 clean-local:
 	-$(RM) -r build
